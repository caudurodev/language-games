table:
  name: test
  schema: public
event_triggers:
  - name: test
    definition:
      enable_manual: false
      insert:
        columns: '*'
      update:
        columns: '*'
    retry_conf:
      interval_sec: 10
      num_retries: 0
      timeout_sec: 60
    webhook: https://local.functions.nhost.run/v1/test
